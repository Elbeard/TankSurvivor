# Prefab чанков и стриминга

Шаблоны, которые создаёт или хранит модуль:

- система стриминга (`ChunkWorldSystem` и т.п.);
- prefab одного чанка по биому (после **Tools → ChunkWorld → …** — когда появится меню).

Игра инстанцирует только ссылки из своей сцены, не дублируя логику модуля.
